package JavaPart3;

public interface IndianBank {
	
	
	//We can not create the method body 
	//We can only declare method in interface
	//We can not create object of the interface 
	//Variables declared in interface are by default static in nature
	//We can not write main method in interface
	//We can not use  static keyword in interface
	//We can not change the variable values it's final in nature
	
	
	public void credit();
	public void debit();
	public void moneytranfer();
	int MinBal=100;
	

}
